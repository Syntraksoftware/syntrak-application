name: snowtrak
description: All in one ski tracking and social app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1
  get_it: ^7.7.0

  # Networking
  http: ^1.1.0
  dio: ^5.4.0

  # Location
  geolocator: ^10.1.0
  permission_handler: ^11.1.0

  # Maps
  google_maps_flutter: ^2.5.0
  flutter_polyline_points: ^2.0.0

  # Storage
  shared_preferences: ^2.2.2

  # Image handling
  image_picker: ^1.0.7

  # UI
  cupertino_icons: ^1.0.6
  intl: ^0.20.2
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/logos/

